## 2026-09-06 引き渡し必須項目：Google Analytics 4

ユーザー指定により、GA4の導入と動作確認をクライアントへの引き渡し条件に追加しました。クライアントが管理権限を持ち、サイトのアクセス・商品閲覧・Shopifyでの購入・問い合わせ送信完了を確認できる構成にします。静的サイトとShopifyの接続、日英表示、計測の重複、テストデータの分離も確認対象です。

**現時点では要件整理のみで、GA4は未設置です。** 導入時に既存プロパティの有無・測定ID・管理者・本番ドメインを確認します。実装方針と完了チェックリストは [docs/analytics-handoff.md](docs/analytics-handoff.md)。この項目が完了するまでGA4の引き渡し完了とは扱いません。

## 2026-09-06 実商品「ルミナス ドーナッツパンダ ティポット」

テスト商品3点を削除し、実商品1点・3種類をShopifyへ登録しました。ストロベリー（KM-2440A）、抹茶（KM-2440B）、チョコ（KM-2440C）は各16,500円、初期在庫各5点です。指定の写真9枚を元解像度でアップロードし、各種類の正面写真を割り当てています。特典・仕様・注意事項と英語訳はShopifyで管理します。国内47都道府県、送料0円の専用配送プロファイルを保存しました。管理先は [docs/luminous-product.md](docs/luminous-product.md)。

商品説明の見出し・箇条書きを保持し、種類選択を長い説明より前に表示します。画像枠・共通ヘッダー・フォント・トップの素材は維持。テスト購入とレイアウトサンプルは無効にしました。ストアはクライアント移行前のテスト決済状態で、実決済ボタンは引き続き無効です。商品登録は実決済開始を意味しません。記事2件・ギャラリー18枚・過去のテスト注文は残しています。

コードのバックアップタグは `backup/pages-before-luminous-product-20260906-000654`（f1cc963）。同時刻のZIP・Git bundle・変更前の日英商品／記事JSONを隣接する `work/backups/` に保存済み。API・購入制御17件、画面30件、9ページの参照検査が合格しました。

## 2026-09-05 お知らせ・商品情報の日英切り替え

英語を Shopify の言語設定に追加し、既存の Japan／myshopify.com ドメインへ割り当て、公開済み。既定は日本語です。言語追加時に公式 Translate & Adapt がインストールされました。通常の商品・記事原稿は日本語のみ、英語のタイトル・本文は同アプリの翻訳欄に保存しています。商品3点・記事2件すべての Storefront API JA／EN を照合済み。記事の画像・キャプションも日英を分離し、ハンドル・公開日時・商品ID・価格・写真・バリエーション・購入制御は維持しています。

既存の `@inContext(language: $language)` と `kmn:languagechange` がネイティブ翻訳を取得するため、公開フロントの改修や翻訳辞書は不要でした。GitHub Pages とローカルデモにCMSから直接反映します。API・購入制御17件、画面27件、9ページ232参照の検証が合格。公開サイトで言語往復・ページ移動・再読み込みを確認済み。操作方法と未翻訳時のフォールバックは [docs/09-content-management.md](docs/09-content-management.md)。

コードの変更前タグは `backup/pages-before-localized-content-20260905-225336`（9af6f6e）。同時刻のZIP・Git bundle・変更前の商品／記事JSONを隣接する `work/backups/` に保存。Gitだけを戻してもShopifyの原稿や言語設定は戻りません。

## 2026-09-05 テストコンテンツと購入導線

お知らせ2件（TESTタグ）と商品3件（kmn-testタグ）をShopifyへ公開。写真15・16・17を使用し、価格1,000・2,000・3,000円、初期在庫各10点はテスト値です。購入テスト後の陶製レース人形の販売可能数は9点です。日英の原稿は上記のネイティブ翻訳登録へ移行済み。管理方法は [docs/test-store-guide.md](docs/test-store-guide.md)。

Shopifyの「テスト決済ゲートウェイサービス」が有効。フロントは `checkoutEnabled:false` のまま、`testCheckoutEnabled:true` と3商品のID・タグを両方検証してテスト購入を開通。テストモードはフロントだけでは強制できないため、本番決済への切替前に必ずテストボタンを無効にしてください。ユーザーがストアパスワードを入力し、承認された宛先でテスト注文の完了を確認しました。今回のユーザー回答で確認メールの受信箱への到達も確認済み。実請求・発送は行っていません。注文詳細や通知先は公開リポジトリに保存しません。

APIと購入制御17件、画面21件、9ページの参照検証が合格。縦長写真が商品枠をはみ出す問題と、商品説明の段落が連結される問題も修正しました。変更前のタグは `backup/pages-before-test-content-20260905-210348`。

## 2026-09-05 窯背景とギャラリーの英語表示

会社案内末尾の仮ラベルを削除し、`kmn-gallery-05` の窯写真を背景に追加。写真濃度と彩度は `tokens.css` の `--cta-photo-*` で管理します。日英共通の背景です。

Shopifyの `gallery_item` に `title_en`・`caption_en`・`category_en` を追加し、18枚すべての英訳を登録済み。`assets/gallery-data.js` が日英の写真情報を選び、ギャラリーと各ページの画像で共用します。CMSの英語入力欄で編集でき、追加アプリは使用していません。拡大表示も英語になり、言語切替時に開いている旧言語の写真ダイアログは閉じます。管理・復元方法は [docs/09-content-management.md](docs/09-content-management.md)。

## 2026-09-05 現行写真の登録

現行ギャラリーの18枚をShopifyのFilesと `gallery_item` に登録済み（Active）。指定された `E:\project-folder\image` を読み取り、ファイル名だけを `kmn-gallery-01` ～ `18` に揃えた同一バイトのコピーをアップロードしました。元フォルダは変更していません。

`mockups/about.html` の制作工程7枠とトップの工房写真もShopify連携済み。`assets/managed-photos.js` が固定ハンドルで取得し、写真差し替え時に追従します。トップは `kmn-gallery-06`。ヒーロー・レース素材・共通ヘッダーは今回変更していません。13件のAPIテストと全9ページのリンク検証が合格。詳細・復元方法は [docs/09-content-management.md](docs/09-content-management.md)。

以下は過去の作業記録です。古い「工房写真は差し替え待ち」「ギャラリー0件」「商品閲覧権限のみ」という記載より上記と管理ドキュメントを優先してください。

## 2026-09-05 コンテンツ管理の追加

お知らせと記事詳細を新デザインへ統一。会社案内も共通ヘッダー・フッターに更新し、ギャラリー・お問い合わせ・送信後ページを追加しました。Shopifyのブログとメタオブジェクトから公開内容を取得します。

操作方法・API権限・お問い合わせの開通状況は [docs/09-content-management.md](docs/09-content-management.md) を参照してください。FormSubmitの初回確認メールを送信し、受信者による有効化完了の回答を受けて、送信ボタンを開通しています。
# 最新状況 2026-09-05

**最新指示: 確認済みローカルデモをGitHub Pagesへ反映。** ユーザーが2026-09-05に明示的に依頼しました。再現素材、境界のぼかし、調整済みのヘッダー余白、Noto Sans JP、同じヘッダー／フッターの商品一覧・詳細、Shopify商品API接続を `mockups/` にコピーしています。反映先は `hirojessica/kmrn` の `main`、公開対象は `mockups/` です。詳細は [docs/08-approved-demo-pages.md](docs/08-approved-demo-pages.md)。

Headlessはインストール済み、ストアフロント369620（Km Nagoya Doll Headless）のStorefront API権限は `unauthenticated_read_product_listings` のみ。公開用トークンは `mockups/assets/storefront-config.js` に設定済みです。非公開・Admin APIトークンを入れないこと。日本語・英語の実APIはHTTP200・商品0件で確認済み。実商品・決済の運用確認はまだです。

今回の反映はローカルデモと同じ内容を公開する作業です。提案画像と完全同一の写真になったことや、Shopifyの販売準備が完了したことは意味しません。本文は仮原稿、工房写真は差し替え待ちです。会社案内・お知らせは旧レイアウトを引き継ぎます。

作業コピー: `C:\Users\Janne\Documents\KMN\work\kmrn-gallery-renewal`。ローカルデモ: 隣接する `kmn-layout-review`。旧公開版はタグ `backup/pages-before-demo-20260905-180843`（891a63e）とZIP・bundleで保存済みです。`theme/`、E:\KMNAGOYAEC、E:\project-folderはこの反映で変更しません。

以下は旧版復元前の履歴です。過去の公開停止・動画・フォント・未着手表記は現在の指示ではありません。

作品写真中心のプレビューとDawnベースのShopifyテーマを実装しました。最新仕様は [docs/06-gallery-preview.md](docs/06-gallery-preview.md)。下記の旧動画仕様・未着手記述は過去時点です。

GitHub Pagesの新デザイン公開と、Shopify下書きテーマID `190615552282` への追加を確認済み。ストアのデフォルト言語は承認を得て日本語へ変更済み。今回の作業チェックアウトは `C:\Users\Janne\Documents\KMN\work\kmrn-gallery-renewal`。E:\KMNAGOYAECと現行サイトのE:\project-folderは変更していません。

# 引き継ぎドキュメント（HANDOFF）

最終更新: 2026-09-03（Codexによる日英切替・ブラウザ言語判定・Googleマップ反映）
引き継ぎ先想定: Codex / Composer 2.5（または Fable 5 の継続セッション）

> **最初に [README.md](README.md) の「AI引き継ぎ時の必読ルール」を読むこと。**
> 要点: 色・余白等は `mockups/assets/tokens.css` の変数のみ使用（直書き禁止）。
> デザインの正は `mockups/` の HTML。ドキュメントと食い違ったらモックが正。

---

## 1. 現在のフェーズ

**デザイン・企画・設計 = 完了。Shopify テーマ実装 = 未着手。**

- 企画書・IA・デザインシステム・ページ仕様・Shopify実装ガイド（docs/01〜05）作成済み
- 静的HTMLモックアップ 6ページ作成済み・ブラウザ表示検証済み（PC/SP）
- `theme\` ディレクトリはまだ存在しない（実装フェーズで作成）

## 2. モックアップの状態（デザインの正）

| ファイル | 内容 | 状態 |
|---|---|---|
| mockups/index.html | トップ（ヒーロー/ピックアップ/会社説明/製造工程/カテゴリ/お知らせ/CTA） | 完成 |
| mockups/about.html | 会社案内（ステートメント/沿革/工程詳細/会社概要/アクセス） | 完成（※沿革の中間年表は要クライアント確認の注記あり） |
| mockups/collection.html | 商品一覧（ピックアップバナー/タブ/グリッド/ページネーション） | 完成 |
| mockups/product.html | 商品詳細（ギャラリー/購入UI/アコーディオン/作り手/関連商品/SP購入バー） | 完成 |
| mockups/news.html | お知らせ一覧 | 完成 |
| mockups/news-article.html | お知らせ記事 | 完成 |

確認方法: `.claude/launch.json` の "mockups"（`python -m http.server 4173 --directory mockups`）
またはHTMLを直接ブラウザで開く。**CSS変更時はブラウザキャッシュに注意**（強制リロード推奨）。

## 3. このフェーズで確定した設計判断（変更禁止。変更時はクライアント確認）

1. **デザインコンセプト**: 「白磁とレースの静けさ、窯の温もり」。磁器白ベース + 明朝見出し + 真鍮アクセント + 青磁CTA（docs/03）
2. **ヒーローの動画/画像切替は運営側（Shopifyテーマエディタ）の設定**。フロントに切替UIは出さない（schema は docs/05 §3）
3. **アイコンは Feather 風ラインSVGで統一。絵文字・アイコンフォント禁止**（docs/03 §6 `.c-icon`）
4. **会社ロゴは `mockups/assets/logo.webp`（王冠+社名、白・透過、609×164）を CSSマスクで着色**して使う。白背景=墨、暗背景=磁器白。ロゴのテキスト組み・白ロゴの明背景直置きは禁止（docs/03 §6 `.c-logo`）
5. **SPフッターは縦積み禁止**: 会社情報=全幅 → Company/Online Shop=2カラム → Support=全幅横並び（docs/02 §5）
6. **SPのお知らせ行は2行構成**（日付+バッジ / タイトル全幅）。バッジは常に `white-space: nowrap`
7. **モバイルメニューは右スライドインのドロワー**（`#drawer` + `assets/drawer.js` が正。docs/02 §2）
8. **お知らせは Shopify 標準ブログ機能（blogs/news）で運用**。分類はタグでバッジ表示（docs/04 §4.5）
9. **購入系CTAは青磁 `--c-celadon` の塗りボタンのみ**。gold は面で使わない（docs/03 §2）
10. **全6ページを日本語・英語で表示**。ヘッダーのJP/ENで手動切替し、選択値を保持。未選択の初回のみブラウザ言語が日本語ならJP、それ以外はEN（静的モックは `assets/language.js`、Shopify本番はMarkets/ロケール機能へ移植）
11. **会社案内のGoogleマップは現行本番サイトと同じ地点・店舗情報を使用**。`about.html` の実iframeが正

## 4. 未完了・TODO

### クライアント確認待ち
- [ ] 沿革の中間年表（about.html に「※年表の詳細はクライアント確認のうえ追記」の仮置きあり）
- [ ] 実際の商品・工房写真（全 `.ph` プレースホルダの差し替え。各プレースホルダ内に必要な被写体・比率を記載済み）
- [ ] ヒーロー動画の素材（窯の炎・レースを泥漿に浸す手元など。poster用静止画も必要）
- [ ] 商品データ（品名・価格・在庫。モック内の商品は全て架空のダミー）
- [ ] 英語表記の正式確認（会社の英語商号、代表者名のローマ字、商品名・ブランド用語）
- [ ] 輸出販売条件の確定（販売国、海外送料、関税・輸入税、返品条件、問い合わせ窓口）

### モック未作成（仕様のみ存在）
- [ ] お問い合わせページ（仕様: docs/04 §4.6）
- [ ] 規約系ページ（特商法・プライバシーポリシー・配送返品 — Shopifyのページ機能で作成想定）

### 実装時に接続するもの（モックでは `#` リンクのまま）
- ヘッダーの検索・アカウント・カート（Shopify標準機能に接続）
- フッターの規約系リンク
- ページネーション・並び替えセレクト・カテゴリタブの実機能
- PCナビ「商品カテゴリ」のドロップダウン（docs/02 §2 で仕様定義済み。モックは collection.html への単リンク）
- 商品詳細のサムネイル切替・数量ステッパーのJS

## 5. 実装フェーズの推奨手順（未着手）

1. Shopify ストア開設（開発ストアで可）+ Shopify CLI セットアップ
2. Dawn 最新版を複製し `theme\` に配置
3. `tokens.css` を `assets/tokens.css` として追加、`theme.liquid` で Google Fonts + tokens を読み込み
4. header / footer セクションをモック通りに改修（ロゴマスク・ドロワー含む）
5. docs/05 §2 の対応表に従い、トップページのセクションから順に Liquid 化（hero-media が最優先・schema は docs/05 §3 に定義済み）
6. コレクション/商品/ブログテンプレート改修 → 初期データ投入（docs/05 §6 チェックリスト）
7. 品質基準の確認（docs/05 §7: Lighthouse・フォーカスリング・alt・税込表記）

## 6. 既知の注意点

- GitHub Pagesの制作中プレビュー用に、`mockups/*.html` 全6ページへ `noindex` を設定済み。Shopify本番公開時にはこの指定を削除すること
- `mockups/assets/language.js` は静的モック用。Shopify本番では翻訳辞書を `locales/*.json` と商品・ページ翻訳へ移し、同JSをそのまま使わないこと
- Python の http.server はキャッシュヘッダを返さないが、ブラウザのヒューリスティックキャッシュで CSS が古いまま表示されることがある。検証時はハードリロードか `?v=` クエリで回避
- `.ph`（プレースホルダ）クラスは実装に持ち込まない（モック専用）
- `[hidden] { display: none !important; }` を base に入れてある（`.ph` 等の display 指定に負けないため）。テーマ実装でも同様の保険を推奨
- ヒーロー動画は `prefers-reduced-motion: reduce` で非表示にし poster を出す CSS を必ず入れる（style.css 末尾に実装例）
